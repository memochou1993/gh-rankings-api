query SearchOwners {
  search(<SearchArguments>) {
    edges {
      cursor
      node {
        ... on User {
          avatarUrl
          createdAt
          followers {
            totalCount
          }
          location
          login
          name
        }
        ... on Organization {
          avatarUrl
          createdAt
          location
          login
          name
        }
      }
    }
    pageInfo {
      endCursor
      hasNextPage
    }
  }
  rateLimit {
    cost
    limit
    nodeCount
    remaining
    resetAt
    used
  }
}
