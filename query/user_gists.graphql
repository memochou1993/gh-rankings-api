query UserRepositories {
  user(<OwnerArguments>) {
    gists(<GistsArguments>) {
      edges {
        cursor
        node {
          name
          stargazers {
            totalCount
          }
        }
      }
      pageInfo {
        endCursor
        hasNextPage
      }
    }
  }
  rateLimit {
    cost
    limit
    nodeCount
    remaining
    resetAt
    used
  }
}
