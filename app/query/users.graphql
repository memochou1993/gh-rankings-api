query Users {
  search(SearchArguments) {
    userCount
    edges {
      cursor
      node {
        ... on User {
          avatarUrl
          createdAt
          email
          followers {
            totalCount
          }
          location
          login
          name
          url
        }
      }
    }
    pageInfo {
      endCursor
      hasNextPage
      startCursor
    }
  }
  rateLimit {
    cost
    limit
    nodeCount
    remaining
    resetAt
    used
  }
}
