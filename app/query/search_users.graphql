query SearchUsers {
  search(<args>) {
    userCount
    edges {
      cursor
      node {
        ... on User {
          login
          name
        }
      }
    }
    pageInfo {
      endCursor
      hasNextPage
      startCursor
    }
  }
  rateLimit {
    cost
    limit
    nodeCount
    remaining
    resetAt
    used
  }
}
